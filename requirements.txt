# Runtime dependencies.
# scikit-learn was previously required for a single MinMaxScaler call; that is
# now three lines in fantrax_salary/model.py and the dependency is gone.
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
requests>=2.31
